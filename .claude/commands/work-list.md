---
description: List all work items with optional filters
argument-hint: [--status STATUS] [--type TYPE] [--milestone MILESTONE]
---

# List Work Items

Run:

```bash
python scripts/session/cli.py work-list $ARGUMENTS
```

Display the output to the user.
