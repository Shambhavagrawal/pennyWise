---
description: Run quality gates validation
argument-hint: [--fix] [--scope backend|frontend]
---

# Validate Quality Gates

Run:

```bash
python scripts/session/cli.py validate $ARGUMENTS
```

Display the output to the user.
